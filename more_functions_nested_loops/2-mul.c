#include "main.h"

/**
 * mul - Multiply two integers.
 * @a: The first value
 * @b: The first value
 *
 * Return: 1 if c is a digit, 0 otherwise
 */


int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
