#include "main.h"

/**
 * _mul - Multiply two integers.
 * @o: The first value
 * @t: The first value
 *
 * Return: 1 if c is a digit, 0 otherwise
 */


int _mul(int o, int t)
{
	int result = o * t;

	return (result);
}
